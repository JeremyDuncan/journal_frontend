/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;


// USE TO DISABLE STRICT MODE (PREVENTS DOUBLE QUERY OF API)
// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     reactStrictMode: process.env.NODE_ENV === 'production',
// };
//
// export default nextConfig;
